[project]
name = "streetsetu-ai"
version = "0.1.0"
description = "Python AI service for StreetSetu"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
